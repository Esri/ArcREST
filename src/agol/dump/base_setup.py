#if __name__ == "__main__":
    #url = "http://services2.arcgis.com/PWJUSsdoJDp7SgLj/ArcGIS/rest/services/GridIndexFeatures/FeatureServer/0"
    #username = "AndrewSolutions"
    #password = "fujiFUJI1"
    #fl = FeatureLayer(url, username, password)
    #chunks = fl._chunks(range(88, 1000), 100)
    ##print fl.maxRecordCount
    #print fl.query(returnCountOnly=True)
    ###for chunk in chunks:
    ###    oid = [str(x) for x in chunk]
    ###    print fl.deleteFeatures(sql='FID in (%s)' % ",".join(oid))
    #print fl.deleteFeatures(sql='FID > 5')
    #print fl.addFeatures(fc=r"c:\temp\AddFeatsTest_Project.shp")
    
    #print fl.query(returnCountOnly=True)
    